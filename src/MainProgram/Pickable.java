package MainProgram;

/**
 * This interface is for objects that can be printed in a list (one line per item) so that you can
 * select a result from a search
 */
public interface Pickable {
    public String getOneLineString();
}
